import 'package:flutter/material.dart';

class InitPopup extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Center(
          child: Text('Init Popup'),
        ),
      ),
    );
  }
}
